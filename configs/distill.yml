seed: 0

distillation:
  temperature: 4
  alpha: 0.7
  temperature_sweep: [1, 2, 4, 8]

training:
  epochs: 50
  batch_size_labeled: 16
  batch_size_unlabeled: 32
  learning_rate: 0.001
  weight_decay: 0.0001

outputs:
  model: model.pt
  results_csv: outputs/logs/temperature_results.csv