seed: 0

model:
  image_size: 96
  num_classes: 7
  dropout: 0.3

training:
  batch_size: 16
  epochs: 30
  learning_rate: 0.001
  weight_decay: 0.0001

optimizer:
  type: adam

scheduler:
  type: cosine